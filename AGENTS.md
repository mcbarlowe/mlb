# AGENTS.md

Repo-specific OMP guidance lives in `.omp/AGENTS.md`.
Use `README.md` for user-facing architecture and `mlb/etl/load_to_database.py` plus `verify_database.py` for the verified PostgreSQL workflow.
